{
  "name": "restaurant",
  "version": "1.0.0",
  "private": true,
  "description": "Restaurant web system — Django + Tailwind CSS",
  "scripts": {
    "build:css": "npx @tailwindcss/cli -i ./static/src/input.css -o ./static/css/app.css --minify",
    "watch:css": "npx @tailwindcss/cli -i ./static/src/input.css -o ./static/css/app.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "tailwindcss": "^4.3.3"
  }
}
